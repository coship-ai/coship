[build]
builder = "dockerfile"
dockerfilePath = "apps/cockpit/Dockerfile"
watchPatterns = ["apps/cockpit/**", "packages/ui/**"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

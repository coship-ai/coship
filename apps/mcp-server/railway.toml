[build]
builder = "nixpacks"
watchPatterns = ["apps/mcp-server/**"]

[deploy]
startCommand = "fastmcp run src/coship_mcp/server.py --transport http --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
